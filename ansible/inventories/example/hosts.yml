---
all:
  vars:
    environment_name: example
  children:
    glpi_app:
      hosts:
        app-node:
          ansible_host: 127.0.0.1
          ansible_connection: local
    glpi_db:
      hosts:
        db-node:
          ansible_host: 127.0.0.1
          ansible_connection: local
