---
all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/glpi_staging_ed25519
    environment_name: staging
  children:
    glpi_app:
      hosts:
        stg-app:
          ansible_host: 192.0.2.10
    glpi_db:
      hosts:
        stg-db:
          ansible_host: 192.0.2.20
